export const developer = "Joma Ipio";
export const title = "R U Next";

export const seo = {
  applicationName: title,
  creator: developer,
  keywords: [
    `${title} Website`,
    `${title} Web Application`,
    `${title} Survival Show`,
    `${title} Promotion`,
    `${title} Promotional Website`,
    `${title} Website`,
    `${title} JTBC`,
    `${title} Be:lift Lab`,
    `${title} Belift`,
  ],
};
